1.将jdb-orm中的映射进行扩展，由枚举改为接口，方便smartone程序扩展新的映射，即业务模型映射，如此一来，将资源和映射的概念统一，而且，可以直接使用jdb-orm提供的映射容器，而不必专门给业务模型提供专门的容器，同时减少在加载资源时因资源类型的不同，而需要专门判断使用不同的容器加载
2.完善jdb-orm的映射容器，提供回滚机制
3.修改了所有底层框架中，与映射相关的代码，包括jdb-orm, jdb-orm-context, jdb-orm-spring, jdb-orm-spring-boot-starter, ibs-parent-code, ibs-i18n, ibs-log, smartone, smt-dev, smt-imp程序
4.调整jdb-orm框架的包结构，使代码和功能的位置更贴切

以上的工作，已经完成了任务2，以及任务5的准备工作


5.优化jdb-orm框架中的方言模块，包括方言的抽象(现在用的是枚举)，以及不同数据库下数据类型的调整，目前数据类型的结构比较混乱，数据类型，数据库数据类型，查询结果数据类型三种交织在一起，对于后期维护不方便，而且特别是数据类型，其实根据最近一段时间的使用，发现其本质上还是数据库数据类型的一种，和实际java中的数据类型并无关系，但是现在这种设计，容易在使用时给人造成误解，所以需要优化
