<?xml version="1.0" encoding="UTF-8"?>
<mapping-configuration>
	<sql namespace="com.test" name="queryUser">
		<content dialect="" type="select"><!-- 如果不写, 默认是当前系统的dialect; all表示所有类型的dialect -->
			select * from sys_user
		
		<!-- 
			<where>
				<if test="name == '哈哈'">
					b
				</if>
			</where>
		-->	
			
		<!-- 
			<set>
				<if test="name == '哈哈'">
					b
				</if>
			</set>
		-->		
		
		<!-- 	
			<trim prefix="where" prefixoverride="" suffix="" suffixoverride="">
				a
				<if test="name == '哈哈'">
					b
				</if>
			</trim> 
		-->
		
		<!-- 
			<switch>
				<if test="name != null">
					and name = #{name}
				</if>
				<if test="sex != null">
					and sex = #{sex}
				</if>
				<else>
					and age = #{age}
				</else>
			</switch>
		-->
		
		<!-- 
			<foreach collection="users" alias="user" open="(" separator="," close=")">
				<if test="user.id != null && user.name == null">
					#{user.id, dataType=string}
				</if>
			</foreach>
			
			<foreach collection="userIds" alias="userId" open="(" separator="," close=")">
				<if test="userId != null">
					#{user.id, dataType=string}
				</if>
			</foreach>
		-->	
		
		</content>
		
		<content dialect="" type="procedure"><!-- 如果不写, 默认是当前系统的dialect; all表示所有类型的dialect -->
			{call test_procedure (
				#{id, dbType=VARCHAR, mode=in},
				#{name, dbType=VARCHAR, mode=out},
				#{queryResult, dbType=CURSOR, mode=out}
			)}
		</content>
	</sql>
</mapping-configuration>